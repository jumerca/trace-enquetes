p><div class="trace-v18-anchor-row">${g.anchors.map(x=>`<span>${v18esc(x)}</span>`).join('')}</div></div><div class="trace-v18-ai-card"><span class="trace-v18-kicker">ÉTAT ACTUEL</span><div class="trace-v18-ai-metrics"><div class="trace-v18-ai-metric"><b>${s.viewed}/${c.evidence.length}</b><span>pièces vues</span></div><div class="trace-v18-ai-metric"><b>${s.keySeen}/${s.key.length}</b><span>preuves clés vues</span></div><div class="trace-v18-ai-metric"><b>${s.links}</b><span>recoupements</span></div><div class="trace-v18-ai-metric"><b>${s.ready}%</b><span>dossier étayé</span></div></div></div><div class="trace-v18-ai-card"><span class="trace-v18-kicker">PROCHAINE ACTION CONSEILLÉE</span><h3>${v18esc(n.title)}</h3><p>${v18esc(n.copy)}</p><div class="trace-v18-ai-actions"><button class="btn primary" onclick="traceV18Go('${n.tab}')">${v18esc(n.label)} →</button><button class="btn" onclick="traceV18AnalyzeSelection()">◆ Analyser mes pièces</button><button class="btn ghost" onclick="traceV18UseHint()">? Indice officiel</button><button class="btn ghost" onclick="traceV18Note(${JSON.stringify(g.title)})">＋ Noter le fil directeur</button></div></div>${logs.length?`<div class="trace-v18-ai-card"><span class="trace-v18-kicker">JOURNAL DE SESSION</span><ul class="trace-v18-log">${logs.map(x=>`<li><span>${new Date(x.t).toLocaleTimeString('fr-FR',{hour:'2-digit',minute:'2-digit'})}</span><b>${v18esc(x.text)}</b></li>`).join('')}</ul></div>`:''}</aside>`)}
window.traceV18OpenAnalyst=traceV18OpenAnalyst;
function traceV18CleanupFloating(){document.getElementById('trace-v18-analyst-btn')?.remove();traceV18CloseAnalyst()}
function traceV18AnalystButton(){traceV18CleanupFloating();if(!currentCase)return;document.body.insertAdjacentHTML('beforeend',`<button id="trace-v18-analyst-btn" onclick="traceV18OpenAnalyst()"><span>A</span><b>ANALYSTE<small>conseil contextuel</small></b></button>`)}
function traceV18Command(){let c=currentCase,p=gp(c.id),ph=v18PhaseData(c,p),n=v18Next(c,p),firstOpen=ph.findIndex(x=>!x.done),active=Math.max(0,firstOpen<0?ph.length-1:firstOpen);return `<section class="trace-v18-command"><div class="trace-v18-command-top"><div class="trace-v18-next"><div class="trace-v18-next-icon">${n.icon}</div><div><small>PROCHAINE ACTION</small><b>${v18esc(n.title)}</b><p>${v18esc(n.copy)}</p></div><button class="btn primary small" onclick="traceV18Go('${n.tab}')">${v18esc(n.label)}</button></div><div class="trace-v18-phases">${ph.map((x,i)=>`<button class="trace-v18-phase ${x.done?'done':i===active?'active':''}" onclick="traceV18Go('${x.id}')"><span><i></i>${String(i+1).padStart(2,'0')}</span><b>${v18esc(x.label)}</b><small>${v18esc(x.sub)}</small></button>`).join('')}</div></div></section>`}
function traceV18Dossier(){let main=document.querySelector('.case-main-v13');if(!main||document.querySelector('.trace-v18-case-strip'))return;let c=currentCase,p=gp(c.id),s=v18Progress(c,p),g=V18_GUIDES[c.id]||{title:'Construire un faisceau',copy:'Observe, recoupe puis conclus.',anchors:[]};main.insertAdjacentHTML('afterbegin',`<div class="trace-v18-case-strip"><div class="trace-v18-focus"><div class="trace-v18-focus-ring" style="--pct:${s.ready}%"><b>${s.ready}%</b></div><div><span class="trace-v18-kicker">FIL DIRECTEUR</span><h3>${v18esc(g.title)}</h3><p>${v18esc(g.copy)}</p><div class="trace-v18-anchor-row">${g.anchors.map(x=>`<span>${v18esc(x)}</span>`).join('')}</div></div></div><div class="trace-v18-timeline"><div class="trace-v18-timeline-head"><b>Chronologie de travail</b><span>${c.timeline.length} REPÈRES</span></div><div class="trace-v18-time-scroll">${c.timeline.map(t=>`<div class="trace-v18-time"><b>${v18esc(t[0])}</b><span>${v18esc(t[1])}</span></div>`).join('')}</div></div></div>`)}
function traceV18Evidence(){let grid=document.querySelector('.evidencegrid');if(!grid||document.querySelector('.trace-v18-lab'))return;let c=currentCase,p=gp(c.id),s=v18Progress(c,p),unlocked=c.evidence.filter(e=>e.stage<=p.stage),list=filteredEvidence(c);grid.insertAdjacentHTML('beforebegin',`<div class="trace-v18-lab"><div class="trace-v18-lab-stats"><div class="trace-v18-lab-stat"><b>${s.unseen}</b><span>à examiner</span></div><div class="trace-v18-lab-stat"><b>${s.keySeen}/${s.key.length}</b><span>clés vues</span></div><div class="trace-v18-lab-stat"><b>${s.pinned}</b><span>épinglées</span></div><div class="trace-v18-lab-stat"><b>${s.links}</b><span>liens établis</span></div></div><div class="trace-v18-lab-actions"><button class="btn primary small" ${s.unseen?'':'disabled'} onclick="traceV18OpenNextEvidence()">Ouvrir la prochaine pièce →</button><button class="btn ghost small" onclick="traceV18Go('tableau')">Voir le faisceau ◆</button></div></div>`);let cards=[...grid.children];cards.forEach((card,i)=>{let e=list[i];if(!e)return;card.classList.add('trace-v18-e-card');let locked=e.stage>p.stage,seen=p.viewed.includes(e.id),label=locked?'VERROUILLÉE':seen?'CONSULTÉE':'NOUVELLE',cls=locked?'lock':seen?'':'new';card.insertAdjacentHTML('afterbegin',`<span class="trace-v18-e-status ${cls}">${label}</span><span class="trace-v18-e-index">${String(i+1).padStart(2,'0')}</span>`)})}
function traceV18Board(){let host=document.querySelector('.board-v9 > .panel');if(!host||document.querySelector('.trace-v18-faisceau'))return;let c=currentCase,p=gp(c.id),defs=availableLinks(c),found=defs.filter(d=>p.links.includes(d.id)),title=id=>c.evidence.find(e=>e.id===id)?.title||id,section=host.querySelector('.sectiontitle');let html=`<div class="trace-v18-faisceau"><div class="trace-v18-faisceau-head"><b>Faisceau actif</b><span>${p.pinned.length} NŒUD${p.pinned.length>1?'S':''} · ${found.length} LIEN${found.length>1?'S':''}</span></div>${p.pinned.length?`<div class="trace-v18-nodes">${p.pinned.map(id=>`<div class="trace-v18-node ${boardSelection.includes(id)?'selected':''}"><span>${v18esc(c.evidence.find(e=>e.id===id)?.type||'PIÈCE')}</span><b>${v18esc(title(id))}</b></div>`).join('')}</div><div class="trace-v18-edges">${found.length?found.map(l=>`<span class="trace-v18-edge">${v18esc(title(l.pair[0]))} ⇄ ${v18esc(title(l.pair[1]))}</span>`).join(''):`<span class="trace-v18-faisceau-empty">Aucun lien établi pour l’instant. Sélectionne au moins deux pièces et lance un recoupement.</span>`}</div>`:`<div class="trace-v18-faisceau-empty">Commence par épingler des preuves depuis l’onglet « Preuves ».</div>`}</div>`;section?.insertAdjacentHTML('afterend',html)}
function traceV18Suspects(){let cards=[...document.querySelectorAll('.suspect-card-premium')];if(!cards.length||document.querySelector('.trace-v18-suspect-tools'))return;cards.forEach((card,i)=>{let s=currentCase.suspects[i],body=card.querySelector('.suspect-body');if(!s||!body)return;body.insertAdjacentHTML('beforeend',`<div class="trace-v18-suspect-tools"><button class="trace-v18-mini" onclick="event.stopPropagation();traceV18NoteSuspect(${JSON.stringify(s.name)})">＋ Ajouter au carnet</button><button class="trace-v18-mini" onclick="event.stopPropagation();traceV18G