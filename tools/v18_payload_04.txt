o('preuves')">▤ Confronter aux preuves</button></div>`)})}
function traceV18EnhanceCase(){if(!currentCase||!document.querySelector('.case-workspace-v13'))return;let header=document.querySelector('.case-banner-v13');if(header&&!document.querySelector('.trace-v18-command'))header.insertAdjacentHTML('afterend',traceV18Command());if(currentTab==='dossier')traceV18Dossier();if(currentTab==='preuves')traceV18Evidence();if(currentTab==='tableau')traceV18Board();if(currentTab==='suspects')traceV18Suspects();traceV18AnalystButton()}
function v18AchievementState(){let solved=CASES.filter(c=>gp(c.id).finished&&gp(c.id).accusation===c.accusation.answer),allKey=CASES.some(c=>{let p=gp(c.id),k=c.evidence.filter(e=>e.key);return k.length&&k.every(e=>p.viewed.includes(e.id))}),links=CASES.some(c=>gp(c.id).links.length>=2),noHint=solved.some(c=>Object.values(gp(c.id).hints||{}).reduce((a,b)=>a+b,0)===0),elite=solved.some(c=>(gp(c.id).score||0)>=95);return{allKey,links,noHint,elite}}
function traceV18EnhanceHome(){traceV18CleanupFloating();let hero=document.querySelector('.v12-hero');if(!hero||document.querySelector('.trace-v18-season'))return;let done=solvedCount(),started=CASES.filter(c=>gp(c.id).started&&!gp(c.id).finished),viewed=CASES.reduce((n,c)=>n+gp(c.id).viewed.length,0),links=CASES.reduce((n,c)=>n+gp(c.id).links.length,0),ach=v18AchievementState();hero.insertAdjacentHTML('afterend',`<section class="trace-v18-season"><div class="trace-v18-season-top"><div class="trace-v18-season-copy"><span class="trace-v18-kicker">CENTRE D’ENQUÊTE · SAISON 01</span><h2>${done===8?'Saison bouclée.':started.length?'Enquête en cours : '+v18esc(started[0].title):'Huit dossiers. Une progression continue.'}</h2><p>${done}/8 affaires résolues · ${viewed} pièces examinées · ${links} recoupements établis.</p><div class="trace-v18-season-stats"><span><b>${done}</b>résolues</span><span><b>${started.length}</b>en cours</span><span><b>${viewed}</b>pièces vues</span><span><b>${links}</b>liens</span></div></div><div class="trace-v18-season-map">${CASES.map(c=>{let p=gp(c.id),r=p.finished?100:p.started?caseReadiness(c,p):0;return`<button class="trace-v18-case-seg ${p.finished?'done':p.started?'active':''}" onclick="openCase('${c.id}')"><b>${c.num}</b><small>${v18esc(c.title)}</small><i style="--p:${r}%"></i></button>`}).join('')}</div></div><div class="trace-v18-achievements"><span class="trace-v18-achievement ${ach.allKey?'on':''}">◉ Œil de lynx · toutes les clés d’un dossier</span><span class="trace-v18-achievement ${ach.links?'on':''}">◆ Faisceau · 2 recoupements</span><span class="trace-v18-achievement ${ach.noHint?'on':''}">? Autonome · dossier sans indice</span><span class="trace-v18-achievement ${ach.elite?'on':''}">★ Expert · score ≥ 95</span></div></section>`);let list=CASES.filter(c=>filter==='all'||filter==='open'&&!gp(c.id).finished||filter==='done'&&gp(c.id).finished||filter==='hard'&&c.difficulty>=4);[...document.querySelectorAll('.v12-case')].forEach((card,i)=>{let c=list[i];if(!c)return;let p=gp(c.id),r=p.finished?100:p.started?caseReadiness(c,p):0;card.classList.add('trace-v18-card');card.insertAdjacentHTML('beforeend',`<div class="trace-v18-card-progress"><i style="width:${r}%"></i></div>`)})}
function traceV18EnhanceReport(){traceV18CleanupFloating();let report=document.querySelector('.report');if(!report||document.querySelector('.trace-v18-grade'))return;let c=currentCase,p=gp(c.id),score=p.score||0,grade=score>=95?'S':score>=88?'A':score>=76?'B':score>=62?'C':'D',hints=Object.values(p.hints||{}).reduce((a,b)=>a+b,0),style=hints===0&&p.wrong===0?'Méthodique':p.links.length>=3?'Analyste':score>=88?'Précis':'Persévérant',hero=report.querySelector('.report-hero');hero?.insertAdjacentHTML('afterend',`<div class="trace-v18-grade"><div class="trace-v18-grade-letter">${grade}</div><div><span class="trace-v18-kicker">ÉVALUATION DE DOSSIER</span><h3>Profil : ${style}</h3><p>Le grade combine ton score final, la solidité du faisceau et l’usage des aides. Il n’empêche jamais de rejouer pour améliorer le dossier.</p><div class="trace-v18-report-badges"><span>${p.links.length} recoupement${p.links.length>1?'s':''}</span><span>${hints} indice${hints>1?'s':''}</span><span>${p.wrong||0} fausse${(p.wrong||0)>1?'s':''} piste${(p.wrong||0)>1?'s':''}</span><span>${score}/100</span></div></div></div>`)}
function traceV18Schedule(kind){requestAnimationFrame(()=>{try{if(kind==='home')traceV18EnhanceHome();else if(kind==='report')traceV18EnhanceReport();else traceV18EnhanceCase()}catch(e){console.warn('TRACE V18 enhancement skipped',e)}})}
const _v18RenderHome=renderHome;renderHome=function(){_v18RenderHome();traceV18Schedule('home')};
const _v18RenderCase=renderCase;renderCase=function(){_v18RenderCase();traceV18Schedule('case')};
const _v18RenderReport=renderReport;renderReport=function(){_v18RenderReport();traceV18Schedule('report')};
const _v18OpenEvidence=openEvidence;openEvidence=function(id){let e=currentCase?.evidence.find(x=>x.id===id),p=currentCase?gp(currentCase.id):null;if(e&&p&&!p.viewed.includes(id))traceV18Log('preuve','Pièce consultée : '+e.title);return _v18OpenEvidence(id)};
const _v18Pin=pin;pin=function(id){let e=currentCase?.evidence.find(x=>x.id===id),p=currentCase?gp(currentCase.id):null,was=p?.pinned.includes(id);if(e&&!was)traceV18Log('pin','Pièce épinglée : '+e.title);return _v18Pin(id)};
if(typeof recoupSelected==='function'){const _v18Recoup=recoupSelected;recoupSelected=function(){let p=currentCase?gp(currentCase.id):null,before=p?.links.length||0,r=_v18Recoup();if(p&&p.links.length>before)traceV18Log('lien','Nouveau recoupement établi');return r}}
const _v18Answer=answerQ;answerQ=function(i,oi){let q=currentCase?.questions[i];if(q)traceV18Log('hypothese',(oi===q.answer?'Déduction validée : ':'Hypothèse testée : ')+q.q);return _v18Answer(i,oi)};
const _v18Hint=hint;hint=function(i){let q=currentCase?.questions[i];if(q)traceV18Log('indice','Indice demandé : '+q.q);return _v18Hint(i)};
if(currentCase&&document.querySelector('.report-shell-v13'))traceV18Schedule('report');else if(currentCase)traceV18Schedule('case');else traceV18Schedule('home');
})();
</script><!-- TRACE V18 INVESTIGATION OS -->